;;; which-key-package.el --- Which-key package configuration

;;; Commentary:

;; Which-key package configuration

;;; Code:

(require 'which-key)
(which-key-mode)

(setq which-key-idle-delay 1)

(provide 'which-key)
;;; which-key-package.el ends here
