;;; undo-tree-package.el --- Undo tree package configuration

;;; Commentary:

;; Undo tree package configuration

;;; Code:

(require 'undo-tree)

(global-undo-tree-mode)

(provide 'undo-tree-package)
;;; undo-tree-package.el ends here
